import TimeEvent from '../utils/TimeEvent';

const timeEvents = [
  new TimeEvent(0, 300, 'Title 1'),
  new TimeEvent(301, 600, 'Title 2'),
  new TimeEvent(601, 900, 'Title 3'),
];

export default timeEvents;